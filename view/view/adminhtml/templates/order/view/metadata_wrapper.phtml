<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

// This template renders the address metadata section with proper title
// Only render if there is actual metadata to display
$childHtml = $block->getChildHtml();
if (trim(strip_tags($childHtml)) && strpos($childHtml, 'No metadata found') === false):
?>
<div class="admin__page-section">
    <div class="admin__page-section-title">
        <span class="title"><?= $block->escapeHtml(__('Address Metadata')) ?></span>
    </div>
    <?= $childHtml ?>
</div>
<?php endif; ?>
